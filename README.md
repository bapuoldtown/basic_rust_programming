This is repsository to know RUST programming fundamentals.
Its focussed on knowing OOPS, Recursion, external dependencies management, RPC calls in Rust.

Vital Cargo commands with Rust
cargo new <projectname>
cargo check ---by defaults checks in src/main.rs of the project
cargo build -- to compile the project
cargo run --- by defaults run the entry command of main.rs fn main()
